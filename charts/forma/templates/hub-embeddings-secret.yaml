{{- if and .Values.hub.enabled .Values.hub.embeddings.enabled (not .Values.hub.embeddings.auth.existingSecret) }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "forma.hubEmbeddingsSecretName" . }}
  labels:
    helm.sh/chart: {{ include "forma.chart" . }}
    app.kubernetes.io/name: {{ include "forma.hubEmbeddingsName" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: hub-embeddings
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/part-of: {{ .Values.partOfOverride | default (include "forma.name" .) }}
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
type: Opaque
data:
  {{ .Values.hub.embeddings.auth.secretKey | default "EMBEDDING_PROVIDER_API_KEY" | quote }}: {{ include "forma.hubEmbeddingsApiKey" . | b64enc }}
  {{- if and (not .Values.hub.embeddings.huggingFace.existingSecret) .Values.hub.embeddings.huggingFace.token }}
  {{ .Values.hub.embeddings.huggingFace.tokenKey | default "HF_TOKEN" | quote }}: {{ .Values.hub.embeddings.huggingFace.token | b64enc }}
  {{- end }}
{{- end }}
