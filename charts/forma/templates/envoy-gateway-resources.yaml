{{- if .Values.envoy.enabled }}
{{- if and (not .Values.envoy.forma.gatewayClass.create) (not .Values.envoy.forma.gatewayClass.name) -}}
{{- fail "envoy.forma.gatewayClass.name is required when envoy.forma.gatewayClass.create=false" -}}
{{- end -}}
{{- $namespace := include "forma.namespace" . -}}
{{- $gatewayName := include "forma.envoy.gatewayName" . -}}
{{- $gatewayClassName := include "forma.envoy.gatewayClassName" . -}}
{{- $proxyName := include "forma.envoy.proxyName" . -}}
{{- $proxyServiceName := include "forma.envoy.proxyServiceName" . -}}
{{- $appSecretName := include "forma.appSecretName" . -}}
{{- $appServiceName := include "forma.name" . -}}
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: EnvoyProxy
metadata:
  name: {{ $proxyName }}
  namespace: {{ $namespace }}
  labels:
    {{- include "forma.labels" . | nindent 4 }}
spec:
  provider:
    type: Kubernetes
    kubernetes:
      envoyDeployment:
        replicas: {{ .Values.envoy.forma.proxy.replicas }}
      envoyService:
        name: {{ $proxyServiceName }}
        type: {{ .Values.envoy.forma.proxy.service.type }}
        labels:
          app.kubernetes.io/name: {{ $proxyServiceName }}
          app.kubernetes.io/managed-by: {{ .Release.Service }}
        {{- if .Values.envoy.forma.proxy.service.annotations }}
        annotations:
          {{- include "forma.tplvalues.render" (dict "value" .Values.envoy.forma.proxy.service.annotations "context" $ ) | nindent 10 }}
        {{- end }}
  logging:
    level:
      default: {{ .Values.envoy.forma.proxy.logging.defaultLevel | quote }}
  telemetry:
    metrics:
      prometheus:
        disable: {{ not .Values.envoy.forma.proxy.telemetry.prometheus.enabled }}
    {{- if .Values.envoy.forma.proxy.telemetry.openTelemetry.enabled }}
      sinks:
        - type: OpenTelemetry
          openTelemetry:
            {{- if .Values.envoy.forma.proxy.telemetry.openTelemetry.host }}
            host: {{ .Values.envoy.forma.proxy.telemetry.openTelemetry.host | quote }}
            port: {{ .Values.envoy.forma.proxy.telemetry.openTelemetry.port }}
            {{- else }}
            backendRefs:
              {{- toYaml .Values.envoy.forma.proxy.telemetry.openTelemetry.backendRefs | nindent 14 }}
            {{- end }}
            resourceAttributes:
              service.name: {{ default $proxyServiceName .Values.envoy.forma.proxy.telemetry.openTelemetry.serviceName | quote }}
              service.namespace: {{ default $namespace .Values.envoy.forma.proxy.telemetry.openTelemetry.serviceNamespace | quote }}
              deployment.environment: {{ default "self-hosted" .Values.envoy.forma.proxy.telemetry.openTelemetry.deploymentEnvironment | quote }}
    {{- end }}
  preserveRouteOrder: true
{{- if .Values.envoy.forma.gatewayClass.create }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: {{ $gatewayClassName }}
  labels:
    {{- include "forma.labels" . | nindent 4 }}
spec:
  controllerName: {{ .Values.envoy.config.envoyGateway.gateway.controllerName | quote }}
  parametersRef:
    group: gateway.envoyproxy.io
    kind: EnvoyProxy
    name: {{ $proxyName }}
    namespace: {{ $namespace }}
{{- end }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: {{ $gatewayName }}
  namespace: {{ $namespace }}
  labels:
    {{- include "forma.labels" . | nindent 4 }}
spec:
  gatewayClassName: {{ $gatewayClassName }}
  listeners:
    - name: {{ .Values.envoy.forma.gateway.listenerName }}
      protocol: HTTP
      port: {{ .Values.envoy.forma.gateway.port }}
      allowedRoutes:
        namespaces:
          from: Same
{{- if .Values.envoy.forma.clientTrafficPolicy.enabled }}
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: ClientTrafficPolicy
metadata:
  name: {{ printf "%s-client-ip" $gatewayName }}
  namespace: {{ $namespace }}
  labels:
    {{- include "forma.labels" . | nindent 4 }}
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: {{ $gatewayName }}
  clientIPDetection:
    xForwardedFor:
      numTrustedHops: {{ .Values.envoy.forma.clientTrafficPolicy.numTrustedHops }}
{{- end }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ printf "%s-health" $proxyServiceName }}
  namespace: {{ $namespace }}
  labels:
    {{- include "forma.labels" . | nindent 4 }}
spec:
  parentRefs:
    - name: {{ $gatewayName }}
      sectionName: {{ .Values.envoy.forma.gateway.listenerName }}
  rules:
    - matches:
        - path:
            type: Exact
            value: /health
      backendRefs:
        - kind: Service
          name: {{ $appServiceName }}
          port: 3000
{{- if .Values.envoy.forma.routes.authCallback }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ printf "%s-auth-callback" $proxyServiceName }}
  namespace: {{ $namespace }}
  labels:
    {{- include "forma.labels" . | nindent 4 }}
spec:
  parentRefs:
    - name: {{ $gatewayName }}
      sectionName: {{ .Values.envoy.forma.gateway.listenerName }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api/auth/callback
      backendRefs:
        - kind: Service
          name: {{ $appServiceName }}
          port: 3000
{{- end }}
{{- if .Values.envoy.forma.routes.v1Client }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ printf "%s-v1-client" $proxyServiceName }}
  namespace: {{ $namespace }}
  labels:
    {{- include "forma.labels" . | nindent 4 }}
spec:
  parentRefs:
    - name: {{ $gatewayName }}
      sectionName: {{ .Values.envoy.forma.gateway.listenerName }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api/v1/client
      backendRefs:
        - kind: Service
          name: {{ $appServiceName }}
          port: 3000
{{- end }}
{{- if .Values.envoy.forma.routes.v2Client }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ printf "%s-v2-client" $proxyServiceName }}
  namespace: {{ $namespace }}
  labels:
    {{- include "forma.labels" . | nindent 4 }}
spec:
  parentRefs:
    - name: {{ $gatewayName }}
      sectionName: {{ .Values.envoy.forma.gateway.listenerName }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api/v2/client
      backendRefs:
        - kind: Service
          name: {{ $appServiceName }}
          port: 3000
{{- end }}
{{- if .Values.envoy.forma.routes.v1Management }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ printf "%s-v1-management" $proxyServiceName }}
  namespace: {{ $namespace }}
  labels:
    {{- include "forma.labels" . | nindent 4 }}
spec:
  parentRefs:
    - name: {{ $gatewayName }}
      sectionName: {{ .Values.envoy.forma.gateway.listenerName }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api/v1/management
      backendRefs:
        - kind: Service
          name: {{ $appServiceName }}
          port: 3000
{{- end }}
{{- if .Values.envoy.forma.routes.v1Webhooks }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ printf "%s-v1-webhooks" $proxyServiceName }}
  namespace: {{ $namespace }}
  labels:
    {{- include "forma.labels" . | nindent 4 }}
spec:
  parentRefs:
    - name: {{ $gatewayName }}
      sectionName: {{ .Values.envoy.forma.gateway.listenerName }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api/v1/webhooks
      backendRefs:
        - kind: Service
          name: {{ $appServiceName }}
          port: 3000
{{- end }}
{{- if .Values.envoy.forma.routes.storage }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ printf "%s-storage" $proxyServiceName }}
  namespace: {{ $namespace }}
  labels:
    {{- include "forma.labels" . | nindent 4 }}
spec:
  parentRefs:
    - name: {{ $gatewayName }}
      sectionName: {{ .Values.envoy.forma.gateway.listenerName }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /storage
      backendRefs:
        - kind: Service
          name: {{ $appServiceName }}
          port: 3000
{{- end }}
{{- end }}
