{{- if and .Values.authzed.enabled .Values.authzed.initialization.enabled (or .Release.IsInstall (and .Release.IsUpgrade .Values.authzed.migrationAcknowledged)) }}
---
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "forma.name" . }}-authzed-initialize
  labels:
    {{- include "forma.labels" . | nindent 4 }}
  annotations:
    argocd.argoproj.io/hook: Sync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation,HookSucceeded
    argocd.argoproj.io/sync-wave: "1"
    helm.sh/hook: {{ ternary "post-install" "pre-upgrade" .Release.IsInstall }}
    helm.sh/hook-weight: "10"
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
spec:
  activeDeadlineSeconds: 900
  backoffLimit: 1
  ttlSecondsAfterFinished: 300
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "forma.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: authzed-initialize
    spec:
      restartPolicy: Never
      {{- if .Values.deployment.nodeSelector }}
      nodeSelector:
        {{- toYaml .Values.deployment.nodeSelector | nindent 8 }}
      {{- end }}
      {{- if .Values.deployment.tolerations }}
      tolerations:
        {{- toYaml .Values.deployment.tolerations | nindent 8 }}
      {{- end }}
      {{- if .Values.deployment.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml .Values.deployment.imagePullSecrets | nindent 8 }}
      {{- end }}
      {{- if .Values.rbac.serviceAccount.enabled }}
      serviceAccountName: {{ .Values.rbac.serviceAccount.name | default (include "forma.name" .) }}
      {{- end }}
      {{- if .Values.deployment.securityContext }}
      securityContext:
        {{- toYaml .Values.deployment.securityContext | nindent 8 }}
      {{- end }}
      containers:
        - name: initialize
          image: {{ include "forma.deploymentImage" . }}
          imagePullPolicy: {{ .Values.deployment.image.pullPolicy }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            readOnlyRootFilesystem: true
            runAsNonRoot: true
          command: ["sh", "-ec"]
          args:
            - |
              attempts=0
              until forma-authzed upgrade prepare; do
                attempts=$((attempts + 1))
                if [ "$attempts" -ge 120 ]; then
                  echo '{"code":"authzed_unavailable","retryable":true,"status":"failed"}'
                  exit 1
                fi
                sleep 5
              done
              forma-authzed upgrade check
          {{- if or .Values.deployment.envFrom (or (and .Values.externalSecret.enabled (index .Values.externalSecret.files "app-secrets")) .Values.secret.enabled) }}
          envFrom:
            {{- if or .Values.secret.enabled (and .Values.externalSecret.enabled (index .Values.externalSecret.files "app-secrets")) }}
            - secretRef:
                name: {{ template "forma.name" . }}-app-secrets
            {{- end }}
            {{- range $value := .Values.deployment.envFrom }}
            {{- if eq .type "configmap" }}
            - configMapRef:
                {{- if .name }}
                name: {{ include "forma.tplvalues.render" (dict "value" $value.name "context" $) }}
                {{- else if .nameSuffix }}
                name: {{ template "forma.name" $ }}-{{ include "forma.tplvalues.render" (dict "value" $value.nameSuffix "context" $) }}
                {{- else }}
                name: {{ template "forma.name" $ }}
                {{- end }}
            {{- else if eq .type "secret" }}
            - secretRef:
                {{- if .name }}
                name: {{ include "forma.tplvalues.render" (dict "value" $value.name "context" $) }}
                {{- else if .nameSuffix }}
                name: {{ template "forma.name" $ }}-{{ include "forma.tplvalues.render" (dict "value" $value.nameSuffix "context" $) }}
                {{- else }}
                name: {{ template "forma.name" $ }}
                {{- end }}
            {{- end }}
            {{- end }}
          {{- end }}
          env:
            {{- if hasKey .Values.deployment.env "DATABASE_URL" }}
            {{- include "forma.envVar" (dict "name" "DATABASE_URL" "value" (index .Values.deployment.env "DATABASE_URL") "context" $) | nindent 12 }}
            {{- end }}
            {{- if hasKey .Values.deployment.env "MIGRATE_DATABASE_URL" }}
            {{- include "forma.envVar" (dict "name" "MIGRATE_DATABASE_URL" "value" (index .Values.deployment.env "MIGRATE_DATABASE_URL") "context" $) | nindent 12 }}
            {{- end }}
            - name: LOG_LEVEL
              value: fatal
            - name: HUB_API_URL
              value: http://localhost
            - name: REDIS_URL
              value: redis://localhost
            - name: ENCRYPTION_KEY
              value: authzed-initialize-unused
            - name: AUTHZED_ENABLED
              value: "true"
            - name: AUTHZED_ENDPOINT
              value: {{ include "forma.authzedEndpoint" . | quote }}
            - name: AUTHZED_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ include "forma.authzedAuthSecretName" . }}
                  key: {{ .Values.authzed.auth.tokenKey }}
            - name: AUTHZED_SYSTEM_KEY
              value: {{ .Values.authzed.systemKey | quote }}
            - name: AUTHZED_INSECURE
              value: {{ include "forma.authzedInsecure" . | quote }}
            - name: AUTHZED_CONSISTENCY
              value: fully_consistent
{{- end }}
