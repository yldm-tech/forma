{{- if .Values.cube.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "forma.cubeName" . }}
  labels:
    helm.sh/chart: {{ include "forma.chart" . }}
    app.kubernetes.io/name: {{ include "forma.cubeName" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: cube
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/part-of: {{ .Values.partOfOverride | default (include "forma.name" .) }}
spec:
  type: {{ .Values.cube.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "forma.cubeName" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.cube.service.port }}
      targetPort: http
      protocol: TCP
{{- end }}
