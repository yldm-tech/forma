// Re-exported from @forma/types so the color/WCAG helpers are defined once and shared
// with the survey renderer (packages/surveys) instead of duplicated per package.
export * from "@forma/types/colors";
