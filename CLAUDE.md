# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

All working instructions live in @AGENTS.md - follow it. It is the single source of truth for this
repository: commands, architecture, testing policy, caching, i18n, tenant scoping, and the conventions CI
enforces. Keep it that way — new repository-wide guidance belongs in `AGENTS.md`, not here.

`apps/web/AGENTS.md` carries one further rule, generated and re-added by `next dev`: read the version-pinned
Next.js docs before writing Next.js code rather than relying on training data. The `nextjs-docs` skill indexes
them.
